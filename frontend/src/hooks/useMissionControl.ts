// Deprecated — use useWebSocket instead.
// Kept as a stub so any lingering imports don't break the build.
import { useWebSocket } from './useWebSocket'

export const useMissionControl = useWebSocket
export default useMissionControl
